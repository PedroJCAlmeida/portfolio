const functionButtons = {
    seeProjects: () => {
      // Lógica para redirecionar para a seção "projects"
      window.location.href = '#projects'; 
      // Ou use a lógica de navegação do React Router, se estiver usando
    },
    // Outras funções para outros botões, se houver
  };
  
  export default functionButtons;